# =============================================================================
# Nuclei Command Center - Docker Environment Configuration
# =============================================================================

# 🔐 AUTHENTICATION (REQUIRED)
# -----------------------------------------------------------------------------
# Generate AUTH_SECRET: openssl rand -base64 32
AUTH_SECRET=CHANGE_ME_TO_A_RANDOM_32_CHAR_STRING

# Generate password hash inside container:
#   docker exec -it nuclei-command-center node -e "require('bcrypt').hash('YOUR_PASSWORD', 12).then(console.log)"
# 
# ⚠️ IMPORTANT: The hash contains $ characters. In docker-compose, use single quotes:
#   ADMIN_PASSWORD_HASH='$2b$12$...'
#
# Or escape each $ with $$:
#   ADMIN_PASSWORD_HASH=$$2b$$12$$...
ADMIN_PASSWORD_HASH=REPLACE_WITH_BCRYPT_HASH

# 🌐 NETWORK CONFIGURATION (OPTIONAL)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for server actions
# ALLOWED_ORIGINS=localhost:3000,192.168.1.100:3000

# Comma-separated list of allowed development origins
# ALLOWED_DEV_ORIGINS=192.168.1.100

# 🛠️ TOOL PATHS (OPTIONAL - Pre-configured in Docker)
# -----------------------------------------------------------------------------
# These are pre-installed in the container, no need to change
# NUCLEI_BINARY=/usr/local/bin/nuclei
# SUBFINDER_BINARY=/usr/local/bin/subfinder
# HTTPX_BINARY=/usr/local/bin/httpx

# 💾 DATABASE (OPTIONAL - Pre-configured in Docker)
# -----------------------------------------------------------------------------
# DATABASE_PATH=/app/data/nuclei.db
