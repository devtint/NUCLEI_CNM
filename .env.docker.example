# Network Config
ALLOWED_ORIGINS=localhost:3000

# Note: Passwords and Secrets are configured via the Setup Wizard (GUI).
# You can override them here if needed (e.g. for CI/CD).
# AUTH_SECRET=
# ADMIN_PASSWORD_HASH=
